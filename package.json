{
  "name": "twoline",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter '@twoline/desktop' dev",
    "build": "pnpm --filter '@twoline/desktop' build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "vitest": "^4.0.18"
  }
}