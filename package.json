{
  "name": "atomic",
  "version": "0.2.2",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter '@atomic/desktop' dev",
    "build": "pnpm --filter '@atomic/desktop' build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "vitest": "^4.0.18"
  }
}